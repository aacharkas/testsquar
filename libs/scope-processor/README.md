# scope-processor

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build scope-processor` to build the library.

## Running unit tests

Run `nx test scope-processor` to execute the unit tests via [Jest](https://jestjs.io).
